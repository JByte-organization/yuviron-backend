name: Deploy backend

on:
  push:
    branches: [dev]

jobs:
  deploy:
    runs-on: [self-hosted, yuviron]

    steps:
      - name: Pull repo
        run: |
          cd /opt/yuviron/src/yuviron-backend
          git pull origin dev

      - name: Rebuild backend
        run: |
          cd /opt/yuviron/infra
          docker compose -f compose.dev.yml up -d --build yuviron-dev-backend